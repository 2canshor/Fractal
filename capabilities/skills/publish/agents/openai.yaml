interface:
  display_name: "Publish"
  short_description: "Deliver a finished outcome safely"
  default_prompt: "Use $publish to deliver this exact finished outcome to the destination I named."
policy:
  allow_implicit_invocation: true
