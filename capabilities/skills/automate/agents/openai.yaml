interface:
  display_name: "Automate"
  short_description: "Make a repeated job run reliably"
  default_prompt: "Use $automate to turn this repeated job into a tested, recoverable workflow."
policy:
  allow_implicit_invocation: true
