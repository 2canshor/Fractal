interface:
  display_name: "Create"
  short_description: "Create one complete requested outcome"
  icon_small: "./assets/create-small.png"
  icon_large: "./assets/create-large.png"
  default_prompt: "Use $create to make the outcome I requested and coordinate the required tools internally."
policy:
  allow_implicit_invocation: true
