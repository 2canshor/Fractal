interface:
  display_name: "Match"
  short_description: "Match the Project Plan to reality"
  default_prompt: "Use $match to correct mismatches between this Project Plan, its resources, and the remaining work."
