interface:
  display_name: "Align"
  short_description: "Align the Project Plan with reality"
  default_prompt: "Use $align to correct misalignment between this Project Plan, its resources, and the remaining work."
policy:
  allow_implicit_invocation: true
