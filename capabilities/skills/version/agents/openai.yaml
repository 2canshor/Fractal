interface:
  display_name: "Version"
  short_description: "Activate one permitted Fractal version"
  default_prompt: "Use $version to apply, verify, record, activate, and publish the exact permitted Fractal change batch."
policy:
  allow_implicit_invocation: false
